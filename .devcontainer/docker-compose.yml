services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    env_file:
      - .env
    volumes:
      - ..:/workspace:cached
    working_dir: /workspace
    command: sleep infinity
