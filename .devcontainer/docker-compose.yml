services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached
      - ${HOME}/.ssh:/root/.ssh
    working_dir: /workspace
    command: sleep infinity
